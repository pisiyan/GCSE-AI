This is the base exam structure, where each number is the number of marks for each questions, and where the questions stored in lists show parent questions. {base_exam_structure}

Keep the exam structure exactly as is. Only change it if the user explicitly requests a different number or type or amount of marks for questions. The total marks must exactly be {marks}.

The user input: "{user_input}"

Return only a valid JSON dictionary with double-quoted keys and lists of numbers as values. Do not add anything else.