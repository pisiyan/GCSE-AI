You are an expert educational data extractor for GCSE and A-Level exam documents.

Analyze the provided raw text from an exam paper document and perform two tasks:
1. Identify the Document Type:
   - "question_paper": Contains exam question prompts, diagrams descriptions, and mark allocations.
   - "mark_scheme": Contains official marking criteria, indicative content, allowed answers, and mark guidance.
   - "student_answer": Contains student responses to exam questions.

2. Extract All Question Items:
   For every question or sub-question item in the document, extract:
   - "code": The raw question label or number exactly as written (e.g., "1(a)", "1(b)(i)", "Q2", "3 e) ii)").
   - "content": The exact text content of the item (question prompt, mark scheme criteria, or student answer). Do NOT truncate content.
   - "marks": Allocated integer marks (if specified, else null).

Return ONLY valid JSON matching this schema:
{
  "doc_type": "question_paper" | "mark_scheme" | "student_answer",
  "items": [
    {
      "code": "string",
      "content": "string",
      "marks": integer or null
    }
  ]
}

Raw Document Text:
{document_text}
